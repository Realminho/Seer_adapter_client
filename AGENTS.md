# Unified AMR Adaptor Codex 지침

## 작업 복구 로그

- 모든 작업 시작 시 `WORKING/YYYY-MM-DD/HHMMSS-{session-name}.md` 파일을 먼저 생성 또는 갱신함
- 날짜는 현재 로컬 날짜 기준으로 작성함
- 시간은 현재 로컬 시작 시각 기준 `HHMMSS` 형식을 사용함
- `{session-name}`은 작업 내용을 짧은 kebab-case로 표현함
- 파일은 계속 수정/삭제되는 임시 작업 복구용이며, 오래 보존할 필요 없음
- 여러 세션이 같은 날짜 파일을 함께 수정하지 않도록 세션마다 별도 파일을 사용함
- 같은 파일명이 이미 있으면 `HHMMSS-{session-name}-2.md`처럼 뒤에 숫자를 붙여 구분함
- 새 작업을 시작해도 다른 세션 파일은 정리하거나 덮어쓰지 않음
- 같은 세션에서 새 작업을 시작할 때만 해당 세션 파일 안의 불필요한 이전 내용을 정리하고 현재 작업만 남김
- 파일 수정 전에는 현재 세션 파일에 `목표`, `지금`, `다음`만 최소로 작성함
- 단계 완료 시 현재 세션 파일의 `완료`와 `검증`을 한 줄씩 갱신함
- 세션 중단 전에는 현재 세션 파일의 `다음`에 재시작할 위치를 반드시 남김

```md
# {session-name}

### 목표
-

### 지금
-

### 완료
-

### 다음
-

### 검증
-
```
