# task1-action-spec-label

### 목표
- `ActionSpec`에 선택적 `label` 필드를 추가한다.

### 지금
- 작업 완료, 결과 보고 준비.

### 완료
- RED 확인 후 `ActionSpec.label`과 회귀 테스트 추가; 커밋 `c504fc9`.

### 다음
- 없음.

### 검증
- GREEN: 19 passed; `git diff --check` 통과; diff는 요청된 2개 파일만 변경.
