# document-hana-simulator

### 목표
- `robots-hana.hcl`을 이용한 1번 로봇 시뮬레이터 실행법을 매뉴얼에 추가한다.

### 지금
- 문서 갱신 및 검증 완료.

### 완료
- `robots-hana.hcl`의 대상 로봇과 실행 명령을 simulator 가이드에 추가했다.
- 실제로 dispatch되는 `run-main.sh` 명령과 옵션 제약도 기록했다.

### 다음
- 없음.

### 검증
- 문서의 상대 링크 대상 파일 존재 확인.
- `git diff --check` 통과.
