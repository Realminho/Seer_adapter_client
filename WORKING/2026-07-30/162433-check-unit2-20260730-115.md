# check-unit2-20260730-116

### 목표
- 2호기 20260730-116의 UmDock 실행 여부와 끝까지 밀고 들어가는 원인을 확인

### 지금
- 20260730-116 UmDock 실행 및 timeout 뒤 JIBOT 동작 잔류 원인 확인 완료

### 완료
- 2호기 실시간 orderId `20260730-116-1`, 목적지 `1_01CH` 확인
- JIBOT `ModeCharge`, task command `charge`, goal `1_01CH`로 UmDock 실행을 확증
- 위치 (10727,-2729), 충전기 목표 좌표까지 약 0.58m 앞에서 `#brake` 정지 확인
- 200초 뒤 `JIBOT_DOCK_FAILED(command=UmDock)`가 발생했지만 JIBOT은 계속 ModeCharge/charge 상태임
- `_run_dock()` timeout 경로가 오류만 만들고 UmStop을 보내지 않아 장애물 해제 뒤 동작 재개 위험이 있음을 확인
- 현재 설정의 UmDock approach params가 주석 상태라 bare UmDock 펌웨어 기본 동작(기본 약 1m reflector approach)을 사용함

### 다음
- 현장에서는 장애물 해제 전 cancelOrder/UmStop으로 ModeCharge를 종료
- 수정 요청 시 dock timeout·실패 경로에서 UmStop 후 정지 확인을 추가하고 회귀 테스트 작성

### 검증
- MQTT 09:30:33Z: DOCKING RUNNING, ModeCharge, charge, charging=false, #brake
- MQTT 09:31:28~38Z 3회: 동일 좌표와 #brake 유지
- MQTT 09:31:53Z: JIBOT_DOCK_FAILED 200s, 그러나 ModeCharge/charge/pathNum=9 유지
