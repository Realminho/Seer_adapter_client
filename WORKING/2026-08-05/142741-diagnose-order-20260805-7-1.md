# diagnose-order-20260805-7-1

### 목표
- order `20260805-7-1` 실행이 멈춘 직접 원인과 근거를 특정한다.

### 지금
- 원격 장비 로그에서 직접 원인을 확인해 진단을 완료했다.

### 완료
- 진단 절차와 조사 범위를 확정했다.
- `192.168.101.62` journal에서 order 접수부터 정지까지의 흐름을 확인했다.
- 14:26:32 유효한 `cancelOrder` 수신 직후 `UmStop` 전송 및 order worker 취소가 실행된 것을 확인했다.
- 로봇 고장/장애물 정지가 아니라 외부 취소 명령에 의한 정상 취소임을 특정했다.
- VDA5050 v3 `instantActions` 스키마에는 FMS 사용자/화면/프로세스 같은 발행 주체 필드가 없음을 확인했다.
- FMS에서 대조 가능한 식별값은 timestamp, headerId=6, actionId=`01KZ864N80AWN1KW1Y9ZWSZ1QG`, 대상 serialNumber=`HN-SH6-TR-002`임을 정리했다.

### 다음
- 필요하면 MQTT broker/ACS 로그에서 cancelOrder 발행 주체를 추적한다.

### 검증
- 14:26:25 p37 `UmGoto`; 14:26:32 cancelOrder→UmStop→worker cancelled; 14:26:38 이후 mode=Stop/status=Stopped/pose 고정.
- `instantActions.schema` 필수 필드: headerId, timestamp, version, manufacturer, serialNumber, actions; action 필드: actionId, actionType, blockingType 등이며 sender/user 식별자는 없음.
