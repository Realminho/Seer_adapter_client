# ultimate2-star-dock-mapping

### 목표
- Ultimate 2의 별 버튼을 modifier로 사용하고 docking 기능을 권장 매핑에 추가한다.

### 지금
- 작업 완료.

### 완료
- ★ modifier로 motor ON/OFF 및 dock/undock 조합을 정의하고 Ultimate Software remap 절차와 docking 제약을 문서화.

### 다음
- 컨트롤러 연결 후 ★ remap 입력 번호를 측정해 `star_modifier_button`을 확정한다.

### 검증
- joystick 테스트 6개, JSON 구문 및 git diff --check 통과. USB receiver 감지, controller는 아직 미연결.
