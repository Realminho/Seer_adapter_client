# auto-select-input-event

### 목표
- 재연결로 바뀌는 event 번호 대신 장치 역할로 Ultimate 2 입력을 선택한다.

### 지금
- event role 자동 선택 구현을 검증한다.

### 완료
- keyboard/controller/mouse 역할 기반 자동 선택과 문서를 추가했다.

### 다음
- 사용자가 role 자동 선택으로 PL/PR/R4 이벤트를 확인한다.

### 검증
- `py_compile`, `--help`, `git diff --check` 성공
