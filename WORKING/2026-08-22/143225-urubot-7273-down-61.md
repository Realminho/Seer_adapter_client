# urubot-7273-down-61

### 목표
- 192.168.101.61 adaptor의 7273 접속 실패 근본 원인 규명 및 해결책 도출

### 지금
- 복구 완료 + watchdog 개선판 작성 완료. 로봇 배포만 남음 (sudo 필요)

### 완료
- urobot·adaptor 모두 계속 active. 죽은 적 없음 (핵심 오해 지점)
- 진짜 원인: VIA Labs USB2.0 허브(usb 1-1)가 안 붙음
  → FTDI FT4232H 미인식 → /dev/ttyUSB0~3 부재
  → bz_robot serialOpen 무한 실패 → JSrvTcp 7273 미개방
  → adaptor 10.8.8.8:7273 ECONNREFUSED
- 부팅별 상관: 커널로그 온전한 11부팅 중 3회 허브 열거 실패, 실패 시 100% 7273 불가
- 8/22 14:30 부팅: usb 1-1 감지됐으나 열거 정지
- 8/22 19:06:49: `usb 1-1: hub failed to enable device, error -62` (ETIME)
- 8/22 19:07:09: 허브 자력 복귀, ttyUSB0~3(FT4232H)+ttyUSB4(pl2303) 전부 붙음
  → 그런데 watchdog이 14:46에 이미 포기해서 urobot 재시작 안 됨
  → USB 살아났는데 7273은 19:31까지 24분간 안 열린 채 방치됨 (실질 손실)
- 8/22 19:31 재부팅: shutdown 시퀀스 전무(마지막 로그 fwupd 기동 중) = 급단전
- 8/22 19:31 부팅(현재): 허브가 커널에 아예 미감지. 19:45:36 watchdog 또 포기

### 복구 (2026-08-22 21:14~21:19)
- xhci-hcd.5.auto unbind/bind 실행 → 21:14:08 허브 재열거, ttyUSB0~4 전부 복귀
- 물리 재연결 없이 **소프트웨어만으로 복구됨** (사람이 로봇에 갈 필요 없었음)
- 21:18:51 urobot 재기동 → 21:19:58 7273 개방 → adaptor 즉시 재연결
- 교훈: 이 장애는 원격 재바인드로 복구 가능. 케이블 접촉 불량 가설은 약해짐
  (드라이버 재바인드로 살아난다 = 허브/케이블이 아니라 SoC xHCI 컨트롤러/포트 상태 문제 쪽)

### watchdog 개선판 (작성 완료, 미배포)
- scripts/urobot-watchdog.sh — ttyUSB 개수로 두 고장을 구분
  - ttyUSB < MIN_SERIAL(4) → urobot 재시작 대신 xhci unbind/bind (부팅당 3회 상한)
  - 재바인드로 포트 복귀 시 래치 해제 + urobot 재시작
  - 포기 래치에 serial=absent/present 기록 → absent로 포기했는데 포트가 돌아오면 자동 해제
    (present로 포기한 건 실제 MCU/E-stop 고장이므로 래치 유지)
- scripts/systemd/urobot-watchdog.{service,timer} — TimeoutStartSec=180 (재바인드 시간 확보)
- scripts/setup-urobot-watchdog-over-ssh.sh — wifi-keeper 설치 스크립트 관례 따름
  - 구 래치 파일 제거함 (빈 gaveup 파일은 신 스크립트가 "present로 포기"로 읽어 영구 묵음됨)
- scripts/test-urobot-watchdog.sh — 7케이스, 변이 5종 전부 검출 확인
- 주석 전부 한국어로 작성. CLAUDE.md에 '주석 작성 규칙' 절 추가 (한국어 전용, 이중 표기 금지,
  기존 영어 주석은 강제 번역 안 함, 로그 문자열·식별자·커밋 메시지는 대상 아님)

### 다음
- 배포: ./scripts/setup-urobot-watchdog-over-ssh.sh ucore@192.168.101.61 (sudo 비번 필요)
- .62에도 동일 배포 검토
- 급단전 재부팅(19:31 등)은 여전히 미해결 별건 — 전원계통 점검 필요
- 주의: "Max number of devices ... 64" 로그는 unbind 해체 중에만 2건 발생 → 슬롯 고갈 근거 아님(해체 경로 부산물)

### 21:09 재확인
- 재부팅 없음 (boot 0 = 19:31:42 그대로, uptime 1:37)
- 이번 부팅 USB 이벤트 0건 — 허브가 단 한 번도 안 나타남
- watchdog 19:45:36 포기 후 손 뗌, xhci 재바인드 시도 흔적 없음
- 즉 "또 죽은" 것이 아니라 19:31부터 계속 같은 장애

### 검증
- ss -ltn | grep 7273 → 없음 (20:19 기준)
- lsusb → 루트 허브 6개만, ttyUSB 없음
- systemctl is-active urobot amr-adaptor → active active
- /run/urobot-watchdog.gaveup 존재, count=3
